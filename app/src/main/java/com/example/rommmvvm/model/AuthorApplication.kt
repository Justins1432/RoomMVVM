package com.example.rommmvvm.model

import android.app.Application

class AuthorApplication : Application() {
    //private val database by lazy { AuthorDatabase.getDatabase(this) }
    //val repository by lazy { AuthorRepository(database.getAuthorDAO()) }
}